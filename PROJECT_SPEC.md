# NectarGate 原蜜桶批入低温熟化前联合检验闭环

## 项目目标

NectarGate 是面向蜂蜜加工合作社质控班组的单节点 Go HTTP 后端，用于管理一次原蜜桶批进入低温熟化罐前的联合检验闭环。系统在锁定时冻结蜂场编号、蜜源花期、摇蜜房批次摘要、原蜜桶号、封签编号、暂存温区、抽样盲码、三联分管数量、玻片编号、DNA 板孔、熟化罐时隙、花粉谱阈值、DNA 阈值、理化阈值、复核人员名单和任务代次；随后推进双人抽样确认、分管封存、资源占用、花粉谱计数、DNA 指纹核验、热损伤与水分复测、异常复判、两名合格人员独立复核，最终只形成可入熟化、已入熟化、质量隔离或已取消中的唯一结论。实现采用 SQLite WAL 持久化目录、任务聚合、盲码映射、资源租约、证据版本链、仪器调用尝试、幂等结果、审计事件和终局凭据，支持进程重启后的确定恢复。规模规划为 24-28 个生产 Go 文件、约 2400-2700 行有效生产 Go 代码，控制在 2000-3000 行，覆盖至少 catalog、inspection、ledger、evidence、arbiter、store、api、cmd 等包，并提供 go.mod 与 linux/amd64、linux/arm64 双架构 Docker 构建。

## 端到端业务流程

1. 建检锁定：提交蜂场、蜜源花期、摇蜜房批次摘要、原蜜桶、封签、暂存温区、盲码、三联分管、玻片、板孔、熟化罐时隙、阈值与复核人员名单；系统校验目录匹配、摘要新鲜度、唯一占用和人员资格后生成任务代次。
2. 双人抽样与分管：两名不同抽样人员按当前代次确认原蜜桶与封签，随后封存三联样本；盲码与原蜜桶号只能在一次性映射闸门中建立，揭盲动作必须由有效阶段触发且不可提前。
3. 资源占用：熟化罐时隙、DNA 板孔和玻片编号通过带逻辑时间的租约登记，占用、换孔和启动操作在并发下原子裁定，失败不留下部分租约。
4. 花粉谱与理化采集：镜检必须覆盖锁定玻片和分类集合，目标花粉、伴生花粉、未知颗粒、污染颗粒按整数计数规则形成覆盖表；HMF、淀粉酶活性、水分、电导率、酸度使用定点整数解析、换算和阈值判断。
5. DNA 核验与复判：qPCR Ct 值写入不可覆盖证据版本链，边界读数、指纹不一致、热损伤疑点、三联样本分歧或理化越界只能创建一个当前代次复判证据，旧代次迟到读数保留但不参与当前裁定。
6. 独立复核与终局：两名不同合格人员复核全部闭合证据后，仲裁器以单写屏障生成唯一入熟化凭据、质量隔离结论或取消结论；终态后的普通操作、读数、复判和复核均被稳定拒绝。

## 核心组件与职责

1. 蜂场蜜源与检验规则目录：维护虚构蜂场、蜜源花期、摇蜜房批次摘要、暂存温区、人员资格、花粉分类集合、DNA 阈值和理化阈值；提供规则版本新鲜度检查。
2. 原蜜入熟化任务聚合：封装状态机、任务代次、幂等操作号、阶段屏障、人员角色约束和终态单写规则，是业务一致性的主边界。
3. 盲码样本与资源占用账簿：管理原蜜桶号到抽样盲码的一次性映射、三联分管一致性矩阵，以及熟化罐时隙、DNA 板孔、玻片编号的并发租约。
4. 花粉谱及理化采集账簿：保存花粉分类计数覆盖格、定点理化读数、派生阈值证据、仪器调用尝试和待重试记录，保证失败不写入有效证据。
5. 花源指纹复判及终局仲裁器：维护 DNA 指纹证据版本链、复判代次、异常归因、独立复核记录和唯一终局凭据。
6. Go HTTP API：提供 JSON 接口、稳定错误码、确定排序原因、SQLite WAL 初始化、启动恢复、健康检查和测试用可控逻辑时钟入口。

## 领域规则与不变量

1. 状态限定为待锁定、待抽样确认、分管封存中、资源占用中、花粉计数中、DNA 核验中、理化复测中、待独立复核、可入熟化、已入熟化、质量隔离、已取消；每个状态只允许定义明确的下一步。
2. 开放任务内原蜜桶号、封签编号、抽样盲码、熟化罐时隙、DNA 板孔和玻片编号必须全局唯一；释放只发生在有效取消、隔离或完成后的归档阶段。
3. 相同操作号和完全相同内容返回首次结果；相同操作号不同内容返回 OPERATION_CONFLICT，且不改变任务。
4. 三联分管必须数量一致、盲码一致、封存人员合法；任一分管缺失、重复或提前揭示会拒绝推进。
5. 花粉计数总和必须等于声明总颗粒数，分类必须来自锁定集合，覆盖表必须覆盖所有锁定玻片；非法计数不写入覆盖格。
6. DNA Ct 值与理化读数统一用固定小数位整数表示，解析必须检查长度、符号、小数位、除零和溢出；边界阈值按锁定规则版本判定。
7. 仪器拒绝、断连、超时和格式错误只记录为可审计待重试调用；重试对象、次数和逻辑时间由测试可控。
8. 抽样人员与复核人员不得重合，两个复核人员也不得相同；复核必须覆盖当前代次全部有效证据。

## 数据模型与持久化

1. FarmSourceRule：蜂场编号、允许蜜源花期、批次摘要有效期、暂存温区范围、人员资格、规则版本。
2. InspectionTask：任务 ID、任务代次、状态、蜂场编号、蜜源花期、摇蜜房批次摘要、原蜜桶号、封签编号、暂存温区、锁定阈值、复核名单、创建逻辑时间。
3. BlindSampleMap：任务 ID、原蜜桶号、抽样盲码、揭示状态、三联分管编号、封存人员、封存逻辑时间。
4. ResourceLease：资源类型、资源编号、任务 ID、任务代次、租约状态、获取逻辑时间、释放逻辑时间。
5. PollenCoverCell：任务 ID、任务代次、玻片编号、分类、计数、覆盖版本、录入人员、有效标记。
6. EvidenceVersion：任务 ID、任务代次、证据类型、关联盲码、玻片编号、板孔、定点读数、派生结论、版本号、不可覆盖标记。
7. AdapterAttempt：仪器类型、调用键、任务 ID、请求摘要、结果类型、重试次数、逻辑时间、原始错误码。
8. ReviewAndFinal：复核人员、复核范围摘要、复判代次、终局类型、凭据编号、单写屏障键。

## 公开接口

1. POST /api/inspections/lock：锁定一次原蜜桶批入熟化检验任务，返回任务 ID、任务代次和占用摘要。
2. POST /api/inspections/{id}/sampling/confirm：提交双人抽样确认，支持操作号幂等。
3. POST /api/inspections/{id}/samples/seal：封存三联分管并建立盲码一致性矩阵。
4. POST /api/inspections/{id}/leases/claim 和 POST /api/inspections/{id}/leases/swap-well：占用或更换熟化罐时隙、板孔、玻片资源。
5. POST /api/inspections/{id}/pollen/counts：提交花粉谱覆盖计数。
6. POST /api/inspections/{id}/evidence/dna 与 POST /api/inspections/{id}/evidence/chemistry：提交 DNA 与理化读数，或触发可控仪器适配器调用。
7. POST /api/inspections/{id}/rejudge：创建当前代次异常复判证据。
8. POST /api/inspections/{id}/reviews 和 POST /api/inspections/{id}/finalize：提交独立复核并竞争生成唯一终局。

## 失败边界

1. 所有写入经单事务完成；锁定、分管、租约、计数、证据、复判和终局任一步失败均回滚到调用前状态。
2. 拒绝响应包含稳定错误码、任务代次、操作号结果和确定排序原因；原因按蜂场、原蜜桶号、盲码、板孔、玻片编号排序。
3. SQLite 使用 WAL、外键和唯一索引约束开放任务占用；启动时重建内存索引并校验未完成租约。
4. 仪器适配器通过接口注入，公开测试使用脚本化拒绝、断连、超时和格式错误，不依赖真实外设。
5. 终态单写屏障由数据库唯一键和聚合状态双重约束保护，并发 finalize 只有一个成功。
6. 旧代次迟到读数和复判会进入审计记录或被拒绝，不能覆盖当前证据链，也不能改变终局。

## 验收标准

1. 锁定时固定蜂场编号、蜜源花期、摇蜜房批次、原蜜桶号、封签编号、暂存温区、抽样盲码、三联分管数量、玻片编号、DNA 板孔、熟化罐时隙、花粉谱阈值、DNA 阈值、理化阈值和任务代次。
2. 每个原蜜桶号、封签编号、盲码、熟化罐时隙、DNA 板孔和玻片编号在开放任务中只能被一个有效任务占用，并发锁定、换孔或启动必须原子裁定。
3. 抽样确认、分管封存、揭盲与复核必须匹配当前任务代次；相同操作号同内容重试幂等，内容冲突报错，陈旧规则或错误蜂场不得推进状态。
4. 花粉镜检必须覆盖锁定玻片和分类集合，目标花粉、伴生花粉、未知颗粒和污染颗粒计数使用确定整数规则，非法计数不得写入有效覆盖格。
5. DNA Ct 值、HMF、淀粉酶活性、水分、电导率和酸度全部使用固定小数位整数运算并检查长度、符号、除零和溢出，算术失败不得写入派生证据。
6. qPCR 仪、分光光度计或水分仪拒绝、断连、超时、格式错误只能形成可审计待重试调用，重试对象、次数和逻辑时间可确定，不得生成合格派生结论或提前释放占用。
7. 出现花源指纹不一致、热损伤疑点、三联样本分歧或理化指标越界时只能创建一个带代次的复判证据，复判须覆盖受影响盲码、玻片和板孔，旧代次迟到读数保持不可覆盖且不影响当前结论。
8. 全部花粉谱、DNA 指纹、热损伤与理化证据闭合并满足锁定阈值，且由两名不同合格人员独立复核后，竞争成功者才可生成唯一入熟化凭据；并发入熟化、质量隔离与取消只能形成一个终局，终态后的读数、复判、复核或普通操作一律被拒绝且不改变状态。

## 确定性测试场景

1. catalog_lock_test.go：蜂场与蜜源花期匹配、批次摘要陈旧拒绝、封签重复拒绝、暂存温区边界通过与越界拒绝。
2. sampling_idempotency_test.go：双人抽样确认成功、抽样与复核人员重合拒绝、相同操作号重试幂等、操作号内容冲突拒绝。
3. lease_concurrency_test.go：并发锁定同一熟化罐时隙、DNA 板孔和玻片时仅一个成功；换孔竞争失败不留下部分占用。
4. blind_sample_test.go：盲码重复拒绝、提前揭示拒绝、三联分管数量不一致拒绝、失败事务不留下分管记录。
5. pollen_counts_test.go：分类覆盖完整成功、计数总数不守恒拒绝、未知分类拒绝、非法计数不写入覆盖格。
6. fixed_decimal_test.go：DNA Ct 边界、HMF 与淀粉酶换算溢出、水分小数位错误、电导率和酸度阈值判定均精确断言。
7. adapter_retry_test.go：qPCR 仪、分光光度计、水分仪的拒绝、断连、超时和格式错误形成确定待重试调用，重启后重试次数和逻辑时间一致。
8. rejudge_final_test.go：复判代次冲突拒绝、旧代次迟到读数隔离、两名独立复核后并发终局只有一个成功、终态后所有普通推进被拒绝。

## 组件追踪关系

1. 蜂场蜜源与检验规则目录覆盖锁定校验、陈旧摘要、蜂场错配、人员资格和阈值冻结。
2. 原蜜入熟化任务聚合覆盖状态迁移、任务代次、幂等操作号、阶段推进和终态屏障。
3. 盲码样本与资源占用账簿覆盖一次性盲码映射、三联分管矩阵、资源租约、并发占用和失败回滚。
4. 花粉谱及理化采集账簿覆盖玻片覆盖表、整数计数守恒、定点读数解析、溢出检查和仪器待重试调用。
5. 花源指纹复判及终局仲裁器覆盖 DNA 版本链、异常复判、独立复核、迟到读数隔离和唯一终局。
6. Go HTTP API 覆盖可运行后端、确定 JSON 错误、SQLite WAL 恢复、公开测试入口、Docker 双架构构建。

## 独特性

本方案专注蜂蜜加工合作社中原蜜桶批进入低温熟化罐前的真实质控闭环，核心复杂度来自盲码一次性映射、三联样本一致性、蜂蜜花粉谱计数、花源 DNA 指纹、HMF 与淀粉酶热损伤判定、低温熟化资源租约和双人复核终局。与此前样例相比，它使用蜂蜜加工中特有的蜜源花期、摇蜜房批次、原蜜桶封签、花粉镜检玻片、qPCR 板孔和理化固定小数证据链，业务对象、检测语义、阈值规则和并发冲突点均独立成型。
